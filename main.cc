#include <bits/stdc++.h>
using namespace std;
typedef vector<int> vi;

int main() {
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) {
        cout << i + 1 << ". Hello World.\n";
    }

    return 0;
}