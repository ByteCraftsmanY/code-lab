#include <bits/stdc++.h>
using namespace std;
typedef vector<int> vi;

int main() {
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) {
        cout << format("{}. Hello World\n", i + 1);
    }
    return 0;
}